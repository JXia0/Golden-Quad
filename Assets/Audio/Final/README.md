# Final Audio Drop Folder

将定稿 `.wav` 文件放在这里，并使用 `Docs/AudioAssetList.md` 中的英文文件名。

Loop 文件导入 Unity 后勾选 `Loop`；短音效保持 `Load Type: Decompress On Load`。

导入完成后执行 Unity 菜单 `Tools > Let Go > Apply Final Art By Filename`，音效、环境循环和主旋律会自动写入五个正式场景。
